languages:
{% if values.languages.java %}
  java:
    version: ${{ values.languages.java.version }}
    package-manager: ${{ values.languages.java.packageManager }}
    build-args:
{% for arg in values.languages.java.buildArgs %}
      - {{ arg }}
{% endfor %}
{% endif %}

{% if values.languages.python %}
  python:
    version: ${{ values.languages.python.version }}
    requirements: ${{ values.languages.python.requirements }}
{% endif %}

# {% if values.languages.javascript %}
#   javascript:
#     node-version: ${{ values.languages.javascript.nodeVersion }}
#     package-manager: ${{ values.languages.javascript.packageManager }}
#     build-script: ${{ values.languages.javascript.buildScript }}
# {% endif %}

# {% if languages.ruby %}
#   ruby:
#     version: ${{ languages.ruby.version }}
#     paths:
# {% for gem in languages.ruby.paths %}
#       - {{ gem }}
# {% endfor %}
# {% endif %}
